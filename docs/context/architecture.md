# Architecture — calisthenics

<!-- TODO(alp): repo is a single 1427-line index.html (embedded CSS+JS) plus README;
no build step or framework. Expand this file only if the project grows past one file —
until then, read index.html directly instead of maintaining a map of it. -->
